const otpTask = require('../otp/otp.task');
const postTask = require('../post/post.task');
const reportTask = require('../report/report.task');
const authTask = require('../auth/auth.task');
const userTask = require('../user/user.task');
module.exports = {
  otpTask,
  postTask,
  reportTask,
  authTask,
  userTask, 
};
